# 数据要素治理网站说明与 V31 生成协调 / 智能混合视觉说明

## 1. 网站是做什么的

这是一个围绕“数据要素治理”主题的 **研究检索 + 证据管理 + 公众号文章写作 + 网络配图 + 成品导出** 网站。

完整流程是：

**确定主题 → 查新闻/政策/论文 → 核验与筛选来源 → 选择或上传证据 → DeepSeek 写作与改稿 → 智能判断“源新闻图 / 网络真实图 / 本地代码解释图” → 网页预览/公众号富文本 → HTML / Word / PDF / Markdown 导出。**

适合持续关注数据产权与确权、公共数据开发利用、数据流通交易、数据资产与入表、可信数据空间、数据基础设施、“数据要素×”场景、数据安全与合规、数据跨境、个人信息保护、AI 数据、训练数据、语料与数据供应链等议题。

## 2. 目前能做什么

### 查询与研究

- 新闻、政策、论文多路检索，可设时间与国内/国际范围。
- 默认国内优先，可切换仅国内、国内外对照、国际优先、仅国际。
- 支持“关键词 + 附加描述”，附加描述用于表达时间、来源、排除项和关注角度，不再把所有限制硬塞进一个长 Query。
- 查询结果展示相关度、权威度、发布时间以及“已核验原文 / 已定位原文”状态。
- 可把查询结果加入文章证据，也可上传 PDF、DOCX、TXT、MD、CSV、JSON、HTML。

### 写作与编辑

- DeepSeek 生成行业观察、政策解读、研究简报、趋势分析、案例拆解等文章。
- 可设置读者、篇幅、语气、标题、结构、开头、段落节奏、证据表达、结尾和生成质量。
- V23 进一步收紧“默认”的含义：默认不是结构选项的别名，而是编辑自由。普通长文通常 0—3 个自然小标题即可；内容连贯时可以完全不用小标题。四段以上过于整齐的默认目录会触发编辑合并。
- 生成后可按句、段落、章节或整篇继续修改，并支持撤回、恢复初稿。
- 默认不显示论文式参考文献；需要正文 [1][2] 来源编号时再手动开启。

### 配图

- 默认使用“**真实来源优先 + 结构化内容按需绘制 + 弱相关真实图拒绝后再画**”的智能混合策略。
- 有已引用新闻/政策/案例时，优先回到确切来源页读取 OpenGraph / JSON-LD / srcset / 正文原图；没有合格原图时才使用 Serper / Google Images。
- 流程、机制、因果、对比、分层、时间线、指标、价值/风险矩阵等内容，可以由本地 Python/Pillow 视觉引擎直接生成解释图；不调用图片生成 API，也不执行模型生成代码。
- 生成页可切换“智能混合 / 真实图片优先 / 解释图优先 / 全部代码绘图 / 仅真实图片”。没有 Serper Key 也可选择全部代码绘图。
- 真实图片保留原图地址、来源名称和来源页；代码图标记“系统根据本文内容绘制”，不会伪装成新闻现场照片。
- 继续过滤二维码、公众号关注卡、logo、头像、占位图、泛科技干扰图，并检查图片可下载性、尺寸、重复和当前段落语义。
- 封面同样改为内容驱动的多视觉方向，不再始终使用同一个固定模板。

### 发布与导出

- 正常预览直接显示正常文章，不显示 DeepSeek 内部 JSON 结构。
- 可复制公众号富文本 HTML，也可直接下载标准 `.html` 文件。
- 可导出 Word `.docx`、PDF 和 Markdown；图片直接嵌入 Word/PDF。
- DOCX / PDF 保留成熟中文文章的字体与阅读层级，但**不再复刻“周报”栏目、01/02 编号、固定“数治观察”框、周报页眉页脚等模板**。


## 2.0 V31：先稳定出文，再独立配图

V31 把生成链路重新拆成两个互不阻塞的阶段：

1. **文章生成阶段**：DeepSeek 只负责标题、正文、事实边界和来源关系。浏览器先拿到 `generationJobId`，通过短轮询等待文章，不再用一条长 HTTP 请求硬等模型，因此部署在 Cloudflare / 反向代理后也不容易出现 524。
2. **视觉阶段**：文章完成并保存后，本地再根据正文段落决定源新闻原图、Serper 网络图或代码解释图。代码绘图不会参与首稿 Prompt，也不会因为画图失败让正文失败。

图片策略仍然保留智能混合、真实图片优先、解释图优先、全部代码绘图、仅真实图片五种模式；只是它们现在属于独立的 `visualSpec`，不会混进写作规格。

源新闻回图也不再依赖 DeepSeek 额外输出图片 DSL：正文有 `[n]` 引用时直接本地绑定对应来源；没有编号时再根据段落与来源标题/摘要/用途做匹配。

## 2.1 V30 智能混合视觉：什么时候找图，什么时候自己画

默认策略不是“凑够用户选择的图片数量”，而是先判断图片位的内容类型。具体新闻、政策发布、企业活动和真实案例优先使用来源原图；真实场景但没有绑定来源时使用 Serper；如果正文在解释流程、机制、因果、对比、架构、时间演进或真实数字关系，代码解释图往往比泛科技图库更准确，因此可以直接绘制。

如果先找真实图但所有候选都被二维码/语义/尺寸/重复门禁拒绝，智能混合与真实图片优先会自动用代码图补位。只有用户选择“仅真实图片”时才允许图片位保持为空。

代码视觉输出为 1600×900 PNG data URI，沿用同一套网页、Word、PDF 图片嵌入链路。模型只提供轻量 `visualIntent / visualType / visualPlan`；实际绘图代码是项目固定实现，不会 `exec` 模型返回的 Python。

## 3. 《数治周报.docx》在本项目里怎么使用

用户提供的文档只作为**写作语言与编辑质量参考**，不是文章模板。

V23 学习的是这类写法：

- 先把可核对的事实或变化讲清；
- 再解释机制、影响与边界；
- 判断克制但明确；
- 多用具体对象、动作、制度和业务场景，少用空泛套话；
- 事实和作者判断能让读者区分开。

明确不再照搬：

- “周报 / 本周导读 / 本期”等栏目口吻；
- `01 / 02 / 03` 固定章节编号；
- “数治观察｜”固定提示框；
- 每节“资料来源”模板；
- “数据要素治理：——……”这种站点主题前缀标题。

“数据要素治理”是网站的选题边界，不是每篇文章标题必须重复的前缀。标题默认从具体事件、变化、冲突、影响或问题自然起题。

## 3.1 V24 查询质量补充：长自然语言 Query

V24 不再把“数据在技术突破上的作用”这类句子当成必须逐字出现的关键词。系统会先抽取“数据”“技术突破”等概念，再生成“数据要素 技术突破”“数据赋能 关键核心技术突破”“数据驱动 技术创新”等短 Query。概念型查询同时走新闻垂类与普通网页发现通道；候选不足时再由 Serper 的网页 + 新闻通道并行补充。

这样既能找到新闻，也能找到国家数据局政策/专家解读、行业媒体网页和中文学术页面。

## 3.2 V24 配图质量补充：源新闻/源材料原图优先

生成阶段最终采用的网络来源会在批量正文抽取时同步提取来源页图片。文章章节若提到该案例、机构、政策或研究，系统先从这些真实来源页图片里挑选；没有合格原图时，才调用 Serper / Google Images。

图片 Query 不再使用过长正文句子，也不再把“原文 配图”作为强制搜索词。来源明确时优先“原文标题 + 媒体/机构”；来源不明确时再找与数据分析、数据平台、科研研发、模型/AI 等直接相关的科技实景或数据可视化。

所有最终图片（包括封面与正文图片）统一在“配图与来源”面板中展示缩略图、来源、尺寸和来源链接。

## 4. 为什么旧版本会“怎么搜都是 0 条”

旧版并不是外部没有新闻，而是多层条件叠加后把已经召回的结果又清空：

1. Query 太长，把年份、媒体、政策、案例等约束拼成一句；
2. 把“数据要素治理”这个上位概念当成标题必须逐字命中的关键词；
3. 近 7 天等时间条件在上游搜索后又被本地严格二次删除，无标准日期的国内结果被误杀；
4. Tavily 曾有不合适的 topic / search_depth 参数组合；
5. 国内政府、媒体、公众号反爬时，被误判成“来源不存在”。

V21 已完成主体查询链路修复，V22 保留这套检索逻辑。

## 5. 当前查询链路

1. **短 Query**：一个搜索句只保留少量核心词。
2. **主题家族展开**：例如“数据要素治理”会展开到数据产权、公共数据、数据流通、数据资产、可信数据空间、数据要素×、AI 数据、语料、数据安全等具体主题。
3. **Tavily 主检索**：新闻、政策、论文分别走合适参数；长自然语言/关系型主题额外并行普通网页检索。
4. **语义相关性门禁**：不要求上位主题或用户整句逐字出现，按核心概念与关系词匹配。
5. **日期容错**：上游已限定时间但结果没有标准发布日期时，不再直接删掉。
6. **来源核验**：能读原文则核验；反爬但索引可信则标记“已定位原文”。
7. **条件式 Serper 补充**：Tavily 中文候选不足时，网页搜索与 News 搜索并行补充，再统一分类与核验。
8. **统一去重排序**：按主题匹配、权威度、时效和国内优先综合排序。

## 6. V22 推文显示与 JSON 修复

DeepSeek 内部仍然采用结构化 JSON 作为机器间传输格式，这是为了稳定拿到标题候选、正文、图片槽位和风险提示；但它属于内部数据，不应出现在用户正文里。

V22 增加两层恢复：

- DeepSeek 的普通文本兜底如果其实返回了 JSON，会先尝试重新解析；
- 即使上游把内容包成 `json { ... }` 放进 `markdown`，服务端也会识别并提取其中真正的 `markdown` 正文，再进入预览和导出。

因此正常生成、网页预览、Word/PDF/HTML 导出都应是普通文章。HTML 导出是标准 HTML 文件，不是 JSON 文件。

## 7. V22 Serper 图片修复

V21 的一个回归问题是：精准模式在“原始报道图片”失败后，备用查询仍然与第一条过于相似，导致多花一次 Serper 请求却没有扩大召回；同时只探测排名最前的少量图片，一旦前两张热链失败，后面的可用图也无法进入正文。

V22 调整为：

1. 有引用来源时先搜“媒体 + 原文标题 + 原文配图”；
2. 第二路改为具体事件 / 章节现场；
3. 第三路放宽到应用案例 / 项目实景；
4. 备用检索只在该图片位确实没有可用图片时触发，而且不会重复第一条查询；
5. 初筛会继续探测到后续候选，避免“前两张失效 = 整个槽位 0 图”；
6. 原始来源站点匹配可作为强语义锚点，减少源头新闻图片被误过滤。

## 8. 导出规则

### 正常文章

- 标题：黑体类中文无衬线字体；
- 正文：宋体类中文衬线字体；
- 正文首行缩进、舒适行距；
- 二级标题按文章自然结构展示，不添加 `01 / 02 / 03`；
- 图片按正文实际位置嵌入并显示来源；
- 默认不附“参考来源”章节。

### 用户显式开启来源编号时

- 正文允许出现 `[1][2]`；
- 网页 / HTML / Word / PDF / Markdown 可附参考来源清单。

## 9. 配置

复制 `.env.example` 为 `.env`：

```env
TAVILY_API_KEY=...
SERPER_API_KEY=...
DEEPSEEK_API_KEY=...
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_BASE_URL=https://api.deepseek.com
HOST=127.0.0.1
PORT=8787
```

- `TAVILY_API_KEY`：主新闻 / 政策 / 论文检索。
- `SERPER_API_KEY`：文章图片搜索；必要时也用于中文新闻/政策补充召回。
- `DEEPSEEK_API_KEY`：检索意图理解、文章生成与二次修改。

## 10. 查询仍为 0 时的排查顺序

1. 访问 `/api/health`，确认 Tavily 已配置。
2. 先用较具体事件词测试，例如“数据产权登记”“可信数据空间”“公共数据授权运营”。
3. 查看结果页警告，区分 API 未配置、上游无结果、配额/网络错误、来源核验拒绝。
4. Tavily 中文候选太少时，确认 `SERPER_API_KEY` 是否可用。
5. 查看服务端日志，不要把媒体、年份、来源等所有要求都塞进关键词框；这些要求应放在“附加描述”。

## 11. V22 验证重点

测试覆盖：

- `json { ... }` 不再泄漏到文章预览；
- “数据要素治理：……”泛化标题前缀会被清理；
- 7 个写作选择器都有独立“默认”选项；
- 默认关闭论文式正文来源编号；
- 搜索页和推文空状态颜文字恢复；
- Serper 精准查询不再重复，且前两张图片不可用时可继续使用后续候选；
- DOCX / PDF 可正常生成、打开和渲染；
- 前端 JavaScript、Python 编译和服务端 smoke test 通过。

真实 API 的最终召回量仍取决于部署环境中的 Key、配额、网络和上游返回内容。

## 12. V23 生成推文专项整改

### 12.1 未上传文件时，自动资料必须真的进入证据包

勾选“自动补充佐证资料”且当前没有上传/引用材料时，链路现在是：

1. 前端用 **主题 + 写作切口** 调 `/api/search`，而不是只搜一个宽泛关键词；
2. 如果前端没有命中，或搜索接口发生非中断错误，文章生成不会被终止；
3. `/api/generate` 在服务端继续执行一次 relevance-first 自动研究，并在候选太薄时只放宽一次；
4. 服务端最终真正采用的资料通过 `sourceList` 回传并同步到左侧“当前证据”；
5. 正文默认仍不显示论文式参考文献，除非用户主动开启来源编号。

因此“自动补充”不再只是后台搜一下，而是能看到哪些资料实际进入了本次写作。

### 12.2 配图不再允许“看起来像科技”就通过

V23 的图片位使用三层约束：

- **文章锚点**：全文主题；
- **章节锚点**：当前小标题；
- **正文锚点**：图片所在位置相邻段落的真实内容。

如果文章本身讨论数据驱动、数据治理、模型或算法，候选图片的标题/来源页元数据还必须出现数据、模型、算法、机器学习、AI、数据分析、仿真、数据平台等实际语义。消费电子评测、泛办公电脑照片、只讲化学结构但没有数据/模型关联的技术图，不再因为“研发”“项目”等泛词而通过。

有明确引用新闻/政策且章节内容与它匹配时，仍优先寻找原报道配图；找不到再做章节级语义搜索。通过不了语义门禁的图片位会留空，**不拿无关图片凑数**。

### 12.3 封面永远有，但不占正文图片数量

封面现在是固定的“第 0 张图”：

- 不计入用户选择的正文图片数量；
- 直接由文章最终标题 + 封面摘要生成编辑设计图；
- 不调用 Serper，因此不会为了封面消耗图片搜索额度；
- 不伪装成新闻现场照片；
- 标题发生变化时，重新生成的封面也跟随主题变化。

正文图片数量只控制正文槽位。选择“正文不配图”时仍然会得到封面。

### 12.4 默认写法不再制造 AI 目录

默认结构的目标不是“自动挑一个模板”，而是像编辑写稿一样先看材料再决定结构：

- 普通文章优先 0—3 个小标题；
- 能用过渡句连接的内容不另起标题；
- 禁止把“问题 / 做法 / 机制 / 原因 / 影响 / 条件 / 判断 / 趋势”当成固定栏目；
- 避免所有小标题长度相近、句式相同；
- 默认开头、段落、证据表达和结尾也允许根据文章内容自由变化。

本地质量门禁会识别六段论式目录；默认模式下，四个以上过于整齐的小节也会进入一次编辑修复。

### 12.5 Word / PDF 与网页预览统一阅读层级

导出不复制《数治周报》的版面，但学习它的中文阅读层级：主标题明显、导语克制、正文宋体类、二级标题黑体类、图片说明小而弱化、页边距和行距舒适。DOCX 使用约 11pt 正文和自然中文行距；PDF 的 ReportLab 路径使用相近字号与 A4 页边距。

如果部署环境缺少 ReportLab，V23 的 Pillow PDF 兜底也重写了：分别选择中文黑体/宋体类运行时字体，重新校准标题、正文、行距、首行缩进、图注与分页，避免 V22 兜底版出现的“大字、同字体、版面松散”。

## 13. V23 验证重点

- 自动资料检索会收到写作切口，并把服务端实际采用来源同步到前端；
- 前端自动检索失败时不会阻断服务端自动补证据；
- 默认四段以上整齐目录会触发编辑修复，六段式 AI 标题被明确识别；
- “笔记本评测照”和“纯化学机理图”不能通过数据驱动文章的图片语义门禁；
- 封面即使正文图片数量为 0、甚至 Serper 未配置也会生成；
- 封面不调用 Serper，降低无意义图片请求；
- DOCX、ReportLab PDF、无 ReportLab 的 PDF 兜底均已实际生成并逐页渲染检查；
- Python / JavaScript 语法、服务端 smoke test 和回归测试通过。



## 3.3 V25 生成页与正文配图补充

生成页桌面端现在以左侧设置栏的真实高度作为右侧文章区域高度，文章过长时在右侧内部滚动，因此不会再出现右侧文档只有一小截、页面下方却留出大块空白的情况。窄屏继续使用自然流式布局。

正文图片进一步强调“先回源、再泛搜”。如果写作模型判断某一图片位对应来源清单中的具体案例、新闻、政策或机构活动，会在图片位写入 `sourceId`，系统直接把这一图片位绑定到对应来源页；先使用来源页原图，再用原报道标题和来源域名定向搜图，最后才使用与当前段落相关的数据科技场景图。图片搜索词同时做短语压缩，避免把主题、小标题和整段正文拼成过长 Query。

“配图与来源”面板会显示封面、已命中的正文图以及未命中的正文图片位；未命中时明确标注，不用无关图片补位。


## 15. V26：从“功能可用”进入“质量守恒”

V26 的原则是：**任何新增质量能力都不能依赖无上限的模型调用或重复搜索来换取**。因此改动优先使用本地语义规则、来源页已有信息、条件式质量门禁和统一导出版式。

### 15.1 查询精度与广度

- 自然语言关系型主题拆成多个语义侧，并使用常见中文表述 OR 组做本地匹配；
- 扩充数据资产融资、训练数据/语料、数据质量、数据标注/合成数据、数据安全合规、数据跨境、个人信息、数据产品、可信数据空间等主题理解；
- 主 Query 优先保留最具体的领域词，而不是自动退化为“数据/数据资产”等大词；
- 普通网页 + News 保持双通道，但 Serper fallback 常规调用预算从 6 次降至 3 次；
- 简单检索说明由本地规则直接解析，只有多约束说明才调用一次意图模型。

### 15.2 文字生成质量

- “默认”明确表示编辑自由，不对应任何固定目录；
- 小标题只有论证真正转折时才出现；
- 增加 AI 标题公式、抽象小标题、重复连接词、碎片段落等本地质量检测；
- 不合格才触发一次定向编辑，合格草稿仍单次生成；
- 来源先抽高信号完整句，减少长网页噪声，降低 Prompt Token 体积；
- 简单写作切口不再额外调用简报模型，多控制项简报才使用规划器。

### 15.3 配图质量与速度

- 有具体案例/新闻来源时，先检查该原始来源页已经抽取到的真实图片；
- 源图合格时不再调用 Serper Images；
- 源图不合格时才用“原文标题/来源 + 当前章节”做一次精确回退；
- 无源图先 1 条短 Query，不再一开始并行两条近似 Query；
- 保留来源图位置并拒绝第 7 张以后的弱可信资源，防止相关文章缩略图、页尾装饰图混入；
- logo / banner / avatar / qrcode / icon / advert 等继续硬过滤；
- 无可靠图片时仍然宁可留空，不使用“看起来科技但和文章无关”的图片。

### 15.4 Word / PDF

Word 作为主要排版源；有 LibreOffice 时 PDF 从最终 DOCX 转换，因此两者共用字体、标题层级、段落、图片和分页逻辑。没有 LibreOffice 时仍可使用 ReportLab 兜底。正文约 11.2pt，黑体类标题、宋体类正文，图片同时限制宽度和高度，避免长图破坏页面。

### 15.5 可重复自查

项目新增 `scripts/quality_benchmark.py`，使用 Mock 做离线前后质量比较，不消耗真实 API。V25→V26 的固定样例中：

- “数据在技术突破上的作用”相关/假阳性分差：22 → 86；
- Tavily 主检索固定样例：5 → 5 次（不增加）；
- Serper fallback：6 → 3 次；
- 无源图初始图片搜索：2 → 1 次；有源图：1 → 0 次；
- 6 条长来源首稿 Prompt 字符代理：24,298 → 13,318（约 -45%）。

完整数据见 `docs/V26_QUALITY_BENCHMARK.md`。


## 16. V27：正文配图召回与匹配质量强化

V27 专门修复 V26 在压缩图片查询调用后出现的召回下降。核心不是“放宽到什么图都能用”，而是让系统更像编辑一样先判断当前小节在讲什么，再决定去哪里找图。

- 小节谈数据治理失败时，搜索数据孤岛、数据质量、数据标准、主数据等真实治理场景；谈普通人时，搜索个人信息保护、隐私、数字政务和个人信息权益；研发、业务、安全、公共数据、数据资产、可信数据空间、AI/训练数据分别使用不同语义路线。
- 长段落不再整句发送给 Google Images，图片 Query 只保留少量核心概念。
- 有案例、新闻或政策来源时，除 Tavily 已抽取的图片外，还会直接从该来源页的 OpenGraph、Twitter、JSON-LD 和正文大图元数据中寻找原图候选，不增加模型 Token 和搜索 API 调用。
- 成功图片位保持单 Query 快速路径；只有失败位置才补第二条不同 Query，并且所有缺图位并行补搜、并行探测，避免质量提升拖慢整体生成。
- 首轮候选探测从 4 个扩展，修复第 5/6 张真实来源图从未被实际下载检查的问题。
- 继续拒绝笔记本评测、CPU/显卡、壁纸、模板、Logo、二维码等与文章数据主题无直接关系的图片。

详细实现与回归结果见 `docs/V27_RELEASE_NOTES.md`。


## 17. V28：二次自查与边界稳定性强化

V28 不改变网站已经稳定的主要交互和调用预算，重点处理真实使用中不容易在 1—3 张图片场景暴露的边界 bug：来源页无 Serper 时仍可取图、多图片位探测公平性、现代网页 `srcset` 图片解析、同域不同文章误认源图、中文相对日期、复杂主题词关系判断、8 个正文图片位绑定保留以及结果 ID 稳定性。

同时重新运行检索质量基准、全部测试、Python/JavaScript 语法检查，并实际生成 DOCX/PDF 后渲染检查。详情见 `docs/V28_SELF_AUDIT.md`。

## 18. V29：二维码硬拦截与段落级配图匹配

V29 的重点不是增加图片搜索次数，而是提高候选图片进入正文之前的质量门槛。现在正文图片要经过三层判断：

1. **来源身份**：真正的新闻/案例文章页仍优先；媒体首页、机构首页不再自动当作“原文页”，避免首页二维码和推广图获得来源加分。
2. **视觉卫生**：URL/alt/class 中的二维码、扫码、公众号、微信、Logo、广告等直接剔除；下载后再用本地像素规则识别 QR finder pattern，解决随机 CDN 文件名二维码漏网。该检查只使用 Pillow，不调用 OCR、视觉模型或额外 API。
3. **段落语义**：正文段落有明确业务场景时，Serper 图片必须和当前段落的局部场景一致。仓库/库存、供应链/物流、制造、销售/订单、财务、医疗、交通等分别有自己的视觉路线，不能只靠“数据治理/AI”大词通过。

这次改动不会增加检索或写作 Token。固定质量基准仍保持 Tavily 5 次主链、Serper 搜索兜底最多 3 次；配图正常路径仍是无来源图 1 次初始图片搜索、来源原图可用时 0 次。完整回归测试为 `107 passed`。

详情见 `docs/V29_IMAGE_PRECISION_AUDIT.md`。

